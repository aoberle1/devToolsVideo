// Using the DevTools Console

let firstNumber = 1;
let secondNumber = 2;

let answer = firstNumber + secondNumber;

// console.log()
console.log( answer );  // comes up with answer of 3

console.log( 'the answer is ' + answer );

console.log( 'the answer is', answer );
// , makes number appear as number in console rather than string